name: crops_recommendation
description: AgriSmart RW — AI crop and fertilizer recommendations for Rwanda.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.2
  cloud_firestore: ^5.6.6
  http: ^1.2.2
  intl: ^0.20.2
  file_picker: ^8.1.7
  google_fonts: ^6.2.1
  shared_preferences: ^2.3.5
  smooth_page_indicator: ^1.2.0+3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
